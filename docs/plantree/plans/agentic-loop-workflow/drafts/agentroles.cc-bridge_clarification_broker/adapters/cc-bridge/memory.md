# CC_BRIDGE Adapter Notes For Clarification Broker

Prefer artifacts suitable for `cc_bridge question` or `cc_bridge plan task-artifact`
imports. Frontdesk owns user presentation; planner owns semantic plan updates.

Never edit `.cc-bridge/runtime`, `.cc-bridge/agents`, lease, socket, pid, mailbox, pane,
provider-state, or tmux files directly.
